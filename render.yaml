services:
  - type: web
    name: multimodal-ai-detector
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HF_API_KEY
        sync: false
